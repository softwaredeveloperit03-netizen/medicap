# eBMR Proceed backend (not executed here — deploy to PHP server)

Use these repo files:

- SQL: `sql/ebmr_filled_step_data.sql` — creates `ebmr_filled_step` + `ebmr_filled_batch_approval`
- API: `php/phpDevelopCyclone/bmr/ebmr_filled_step_api.php` — copy next to `bmr/process.php`

Angular saves/fetches via `BmrFormStorageService` → `bmr/ebmr_filled_step_api.php?type=...`
